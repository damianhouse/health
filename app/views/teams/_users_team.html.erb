
<table class="table table-responsive table-hover team">

    <% if @user.coach != nil %>
      <% coach = @user.coach %>
      <% user_convos = coach.conversations.where(user_id: @user.id) %>
      <tr>
        <td class="title">
          <h2>Coach 1</h2>
          <div class="msg"><i class="status msg new"></i><span>Msg</span></div>
        </td>
        <td class="avatar">
          <!-- <span class="badge"><%=image_tag coach.avatar_url, class: "center-block"%></span> -->
          <% if coach.avatar_url != nil %>
            <img class="avatar" src="<%= coach.avatar_url %>" alt="coach avatar"/>
          <% else %>
            <img class="avatar" src="/assets/generic_avatar.jpg" alt="alt avatar"/>
          <% end %>
        </td>
        <td class="details">
          <div class="name"><%= coach.first %> <%= coach.last %></div>
          <div class="email"><%= coach.email %></div>
          <div class="details">
            <%= link_to 'View Profile', coach %>
          </div>
        </td>
        <td class="convo">
          <% if user_convos.length >= 1 %>
            <% user_convos.each do |convo| %>
              <span><%= link_to 'View Conversation', convo %></span>
            <% end %>
          <% else %>
            <%= render partial: "users_conversation_form", locals: { coach: coach} %>
          <% end %>
        </td>
      </tr>
    <% end %>

    <% if @user.find_coach_1 != nil %>
      <% coach = @user.find_coach_1 %>
      <% user_convos = coach.conversations.where(user_id: @user.id) %>
      <tr>
        <td>
          <% if coach.avatar_url != nil %>
            <span class="badge"><img src="<%= coach.avatar_url %>" alt="coach avatar" style="width:75px;height:75px;" /></span>
          <% else %>
            <span class="badge"><img src="/assets/generic_avatar.jpg" alt="alt avatar" style="width:75px;height:75px;" /></span>
          <% end %>
        </td>
        <td>
          <span class="orange_text"><%= coach.first %></span>
          <span class="orange_text"><%= coach.last %></span>
        </td>
        <td>
          <span><%= link_to 'Coach Details', coach %></span>
        </td>
        <td>
          <% if user_convos.length >= 1 %>
            <% user_convos.each do |convo| %>
              <span><%= link_to 'View Conversation', convo %></span>
            <% end %>
          <% else %>
            <%= render partial: "users_conversation_form", locals: { coach: coach} %>
          <% end %>
        </td>
      </tr>
    <% end %>

    <% if @user.find_coach_2 != nil %>
      <% coach = @user.find_coach_2 %>
      <% user_convos = coach.conversations.where(user_id: @user.id) %>
      <tr>
        <td>
          <% if coach.avatar_url != nil %>
            <span class="badge"><img src="<%= coach.avatar_url %>" alt="coach avatar" style="width:75px;height:75px;" /></span>
          <% else %>
            <span class="badge"><img src="/assets/generic_avatar.jpg" alt="alt avatar" style="width:75px;height:75px;" /></span>
          <% end %>
        </td>
        <td>
          <span class="orange_text"><%= coach.first %></span>
          <span class="orange_text"><%= coach.last %></span>

        </td>
        <td>
          <span><%= link_to 'Coach Details', coach %></span>
        </td>
        <td>
          <% if user_convos.length >= 1 %>
            <% user_convos.each do |convo| %>
              <span><%= link_to 'View Conversation', convo %></span>
            <% end %>
          <% else %>
            <%= render partial: "users_conversation_form", locals: { coach: coach} %>
          <% end %>
        </td>
      </tr>
    <% end %>

    <% if @user.find_coach_3 != nil %>
      <% coach = @user.find_coach_3 %>
      <% user_convos = coach.conversations.where(user_id: @user.id) %>
      <tr>
        <td>
          <% if coach.avatar_url != nil %>
            <span class="badge"><img src="<%= coach.avatar_url %>" alt="coach avatar" style="width:75px;height:75px;" /></span>
          <% else %>
            <span class="badge"><img src="/assets/generic_avatar.jpg" alt="alt avatar" style="width:75px;height:75px;" /></span>
          <% end %>
        </td>
        <td>
          <span class="orange_text"><%= coach.first %></span>
          <span class="orange_text"><%= coach.last %></span>
        </td>
        <td>
          <span><%= link_to 'Coach Details', coach %></span>
        </td>
        <td>
          <% if user_convos.length >= 1 %>
            <% user_convos.each do |convo| %>
              <span><%= link_to 'View Conversation', convo %></span>
            <% end %>
          <% else %>
            <%= render partial: "users_conversation_form", locals: { coach: coach} %>
          <% end %>
        </td>
      </tr>
    <% end %>

    <% if @user.find_coach_4 != nil %>
      <% coach = @user.find_coach_4 %>
      <% user_convos = coach.conversations.where(user_id: @user.id) %>
      <tr>
        <td>
          <% if coach.avatar_url != nil %>
            <span class="badge"><img src="<%= coach.avatar_url %>" alt="coach avatar" style="width:75px;height:75px;" /></span>
          <% else %>
            <span class="badge"><img src="/assets/generic_avatar.jpg" alt="alt avatar" style="width:75px;height:75px;" /></span>
          <% end %>
        </td>
        <td>
          <span class="orange_text"><%= coach.first %></span>
          <span class="orange_text"><%= coach.last %></span>
        </td>
        <td>
          <span><%= link_to 'Coach Details', coach %></span>
        </td>
        <td>
          <% if user_convos.length >= 1 %>
            <% user_convos.each do |convo| %>
              <span><%= link_to 'View Conversation', convo, class:"btn btn-default" %></span>
            <% end %>
          <% else %>
            <%= render partial: "users_conversation_form", locals: { coach: coach} %>
          <% end %>
        </td>
      </tr>
    <% end %>

</table>
